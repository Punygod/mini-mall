<view class="container">
    <view class="title">{{fence.title}}</view>
    <view class="row-container">
        <block wx:for="{{fence.cells}}" wx:key="{{index}}">
            <s-cell class="cell" cell="{{item}}" y="{{index}}" x="{{x}}"></s-cell>
        </block>
        <view class="hr"></view>
    </view>
</view>
